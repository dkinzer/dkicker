Create local and production files to export environment variables before
running the git-hooks.  Copy ./config/env/local.example to .config/env/local
and .config/env/production to change default environment variables.

